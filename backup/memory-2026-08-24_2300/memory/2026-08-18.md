# 2026-08-18 — Дневник

## DeepSeek allowlist — ИСПРАВЛЕНО (08:02-08:08 UTC)
Артур дал задачу: починить allowlist моделей, из-за которого падали cron-задачи (news-digest и др. отваливались с «deepseek/deepseek-chat rejected by agents.defaults.models allowlist»).

Сделано:
- Бэкап конфига перед правкой (в /root/.openclaw-neuro/, несколько .bak снапшотов).
- В `/root/.openclaw-neuro/openclaw.json` → `agents.defaults.models` добавлены:
  - `deepseek/deepseek-v4-pro` (alias `deepseek`)
  - `deepseek/deepseek-chat` (alias `deepseek-chat`)
- Проверено сырым выводом, список обновился (были только Claude/Groq/OpenRouter, теперь есть deepseek).
- `agents.defaults.model.primary` уже был `deepseek/deepseek-v4-pro` (fallback `anthropic/claude-sonnet-5`) — не менял.

## Cron keti_reminders_check — переключён на DeepSeek
- jobId `e83762ec-857d-4c0b-9327-662255658171`
- payload.model: `haiku` → `deepseek/deepseek-chat`
- (это «пункт 2» из плана Артура)

## Хелпер-бот (пункт 1) — уже на DeepSeek
- Профиль хелпера: `/root/.config/openclaw/profiles/helper.json` — `agents.helper.primaryModel: deepseek/deepseek-chat` (уже стоит).
- `/root/.openclaw-helper/openclaw.json` — `models.providers` пустой (`{"providers":{}}`), agents.list пустой.
- Вывод: хелпер-бот уже работает на deepseek-chat, менять не пришлось.
- Хелпер — отдельный gateway на порту 18790 (openclaw-gateway-helper.service), свой бот-токен.

## news-digest-automation-vibecoding — allowlist больше не блокирует
- После правки allowlist прогон перестал падать с отказом по allowlist (раньше падал за ~25мс).
- НО появилась НОВАЯ ошибка: «isolated agent setup timed out before runner start» (таймаут 60с на подготовке изолированного агента).
- Это отдельная проблема, НЕ allowlist. Требует разбора (задача не закрыта).

## НОВАЯ ЗАДАЧА: кнопки переключения модели (08:08 UTC)
ТЗ получено, файл: `/root/.openclaw-neuro/media/inbound/tz-model-switch-buttons_1---187ceed7-f619-4c11-bbda-f18a7ad3b8df.md`

Суть:
- Две inline-кнопки у Джарвиса:
  - «💬 Спросить (DeepSeek)» → `deepseek/deepseek-v4-pro`
  - «🔧 Собрать проект (Sonnet 5)» → `anthropic/claude-sonnet-5`
- Кнопки видны ПОСТОЯННО, не пропадают после нажатия.
- При нажатии — короткое подтверждение смены модели.
- Использовать тот же механизм переключения, что и `/model` (не дублировать логику).
- Модель по умолчанию = `deepseek/deepseek-v4-pro` (уже в primary).
- Definition of done: обе кнопки видны + живой тест + сырой вывод конфига до/после.

Прогресс по кнопкам: ТЗ прочитано, начал штудировать доки OpenClaw по inline-кнопкам Telegram:
- `channels.telegram.capabilities.inlineButtons` (scopes: off/dm/group/all/allowlist)
- message action с `buttons: [[{text, callback_data}]]` или `presentation.blocks[].type:"buttons"`.
- Callback-клики, не захваченные плагином, приходят агенту текстом `callback_data: <value>`.
- Документы читал: /usr/lib/node_modules/openclaw/docs/channels/telegram.md (inline buttons, message actions), plan/ui-channels.md (message presentation contract).

## Важные заметки сессии
- Runtime модели: текущая сессия = `deepseek/deepseek-v4-pro` (после сброса на default в начале).
- Сбрасывал модель сессии через session_status(model=default).
- Канал Telegram direct, capabilities=inlinebuttons,nativeapprovals.
## ДОПОЛНЕНИЕ (12:11 UTC) — статус кнопок + news-digest

### news-digest: allowlist починен, но есть новая ошибка (НЕ allowlist)
- Два принудительных прогона (runMode=force) news-digest-automation-vibecoding (jobId `60991ed9-761f-40e0-a690-a2a4d4c1f3d8`, payload.model `deepseek/deepseek-chat`, sessionTarget `session:agent:main:telegram:direct:199790247`):
  - 1-й: «isolated agent setup timed out before runner start» (таймаут 60с)
  - 2-й: «CronSessionLifecycleClaimError: Session changed while starting work. Retry.» — конфликт из-за того, что cron целится в ту же активную сессию (agent:main), где я работаю.
- Allowlist больше НЕ блокирует (ошибка allowlist ушла). Это отдельная проблема сессионной блокировки/таймаута, разбор продолжу.

### Кнопки: найден рабочий механизм (важно для реализации)
- **`action.type: "command"`** с `command: "/model deepseek/deepseek-v4-pro"` → кнопка запускает НАТИВНУЮ slash-команду через core command path. Это и есть «тот же механизм, что /model» — без дублирования логики.
- **`action.type: "callback"`** с `value` → клик приходит агенту текстом `callback_data: <value>` (если не захвачен плагином).
- Кнопки задаются через message action: либо `buttons: [[{text, callback_data}]]`, либо `presentation.blocks[].type:"buttons"`.
- Telegram: `channels.telegram.capabilities.inlineButtons` (scopes off/dm/group/all/allowlist, default allowlist). runtime capabilities = `inlinebuttons,nativeapprovals` → inline-кнопки ДОСТУПНЫ. «Rich messages» (таблицы/details) отключены, но это другое, кнопкам не мешает.

### План реализации кнопок (ещё не сделано)
1. Кнопки через `action.type: "command"` → `/model deepseek/deepseek-v4-pro` и `/model anthropic/claude-sonnet-5`.
2. «Видны постоянно» — вариант: pinned-сообщение с кнопками, или кнопки прикрепляются к каждому ответу. (Уточнить/решить.)
3. Подтверждение смены модели — уже даёт сам обработчик /model (короткий ответ о смене).
4. agents.defaults.model.primary уже = deepseek/deepseek-v4-pro (по ТЗ это и есть дефолт).
5. Живой тест обеих кнопок + сырой вывод конфига до/после.

### Файлы/конфиг (текущее состояние)
- Главный конфиг: `/root/.openclaw-neuro/openclaw.json` (вне sandbox read-тула, читать через exec).
- `agents.defaults.model.primary = deepseek/deepseek-v4-pro`, fallbacks `[anthropic/claude-sonnet-5]`.
- Агенты: main (deepseek-v4-pro), kent/galaktik/keti (deepseek-chat).
- Профиль хелпера: `/root/.config/openclaw/profiles/helper.json` (primaryModel deepseek-chat, уже на DeepSeek).

## ДОПОЛНЕНИЕ (13:42 UTC) — inline-кнопки отправлены, но ТЗ поменялось на ReplyKeyboardMarkup

### Что было сделано после предыдущего апдейта (12:11 UTC)
- Реализованы и отправлены inline-кнопки (action.type:"command") в Telegram чат Артура (199790247):
  - «💬 Спросить (DeepSeek)» → command `/model deepseek/deepseek-v4-pro`
  - «🔧 Собрать проект (Sonnet 5)» → command `/model anthropic/claude-sonnet-5`
- Callback_data формируется как `tgcmd:/model <ref>` — 37 и 38 байт, укладывается в лимит Telegram 64 байта.
- Первая попытка: отправлено ЗАКРЕПЛЁННОЕ (pinned) сообщение с кнопками через `openclaw message send --channel telegram --target 199790247 --pin --json` — messageId 13200, handledBy: plugin.
- Проблема: Telegram в плашке закреплённого сообщения показывает только текст, кнопки не видны в pinned-баре — это стандартное поведение Telegram, не баг OpenClaw.
- Отправлено второе, обычное (не pinned) сообщение с теми же кнопками — кнопки видны под этим сообщением, но не «навсегда» (уедут вверх при новых сообщениях).
- Объяснил Артуру ограничение: inline-кнопки Telegram всегда привязаны к конкретному сообщению, «вечных» инлайн-кнопок не бывает. Единственный способ иметь кнопки постоянно на экране — ReplyKeyboardMarkup (клавиатура у поля ввода).

### НОВОЕ ТЗ от Артура (13:31 UTC) — заменить inline-кнопки на ReplyKeyboardMarkup
Задача: заменить inline-кнопки на постоянную Reply Keyboard.
Требования:
1. Использовать `ReplyKeyboardMarkup` (НЕ InlineKeyboardMarkup) с `resize_keyboard: true`.
2. Те же две кнопки: "💬 Спросить (DeepSeek)" и "🔧 Собрать проект (Sonnet 5)".
3. Reply Keyboard отправляет текст кнопки как обычное сообщение от пользователя (не callback) — обработчик должен реагировать на этот текст как на команду смены модели (связать с той же логикой /model).
4. Отправить клавиатуру один раз (например при /start или первом сообщении) — должна остаться закреплённой у поля ввода до явного ReplyKeyboardRemove.
5. Подтвердить сырым выводом, что кнопки видны постоянно (не только под одним сообщением).

### Статус реализации ReplyKeyboardMarkup — НЕ СДЕЛАНО (заблокировано инструментами)
- На момент этого апдейта я нахожусь в pre-compaction memory flush обороте, где доступны ТОЛЬКО read/write в sandboxed workspace. exec, message send, cron, session_status — недоступны.
- Реализация ReplyKeyboardMarkup требует:
  1. Проверить, поддерживает ли OpenClaw telegram-плагин отправку `reply_markup: {keyboard: [[...]], resize_keyboard: true}` (обычная Reply Keyboard, не inline) — искать в доках/коде плагина telegram (`docs/channels/telegram.md`, `docs/plugins/message-presentation.md` описывают только inline buttons/callback/command actions; нужно проверить, есть ли отдельный путь для ReplyKeyboardMarkup или потребуется кастомный хук/плагин).
  2. Если штатной поддержки нет — написать небольшой плагин/хук, который: (a) при первом сообщении или /start шлёт message с reply_markup типа keyboard (не inline_keyboard), (b) перехватывает входящий текст, равный тексту кнопки ("💬 Спросить (DeepSeek)" / "🔧 Собрать проект (Sonnet 5)"), и транслирует его в тот же код, что обрабатывает `/model deepseek/deepseek-v4-pro` / `/model anthropic/claude-sonnet-5" (переиспользовать, не дублировать).
  3. Разослать через нативный send или через прямой Telegram Bot API вызов, если message-presentation контракт не поддерживает keyboard-тип (уточнить границы существующего contract).
- Ничего из этого не выполнено в этом обороте — только зафиксирован план. Требуется полный набор инструментов (exec как минимум) для чтения кода telegram-плагина и правки/отправки.

### Next steps (после снятия ограничений оборота)
1. Прочитать исходники telegram-плагина OpenClaw (искать через exec: `grep -r "reply_markup\|ReplyKeyboardMarkup\|resize_keyboard" /usr/lib/node_modules/openclaw/`) — понять, поддерживается ли keyboard-тип нативно.
2. Если нет нативной поддержки — реализовать хук/плагин, который шлёт `sendMessage` с `reply_markup: {keyboard: [["💬 Спросить (DeepSeek)", "🔧 Собрать проект (Sonnet 5)"]], resize_keyboard: true}` напрямую через Bot API (используя токен `@Djarvis_Fobs_helper_bot` или актуальный бот-токен главного канала — уточнить, какой бот используется в текущей telegram-сессии Артура).
3. Добавить обработчик входящих текстовых сообщений, точно совпадающих с текстом кнопок, который вызывает тот же код, что `/model deepseek/deepseek-v4-pro` и `/model anthropic/claude-sonnet-5` (не дублировать логику — найти функцию-обработчик команды /model и вызвать её напрямую).
4. Отправить клавиатуру Артуру один раз.
5. Живой тест: нажать обе кнопки Reply Keyboard, убедиться что текст приходит и модель реально переключается + короткое подтверждение.
6. Показать сырой вывод (JSON запроса sendMessage с reply_markup) до/после, подтвердить что клавиатура видна постоянно (скриншот или описание — попросить Артура подтвердить визуально, раз это Telegram UI).

## ДОПОЛНЕНИЕ (13:56 UTC) — новое упрощённое ТЗ + находка по API переключения модели

### Новое ТЗ от Артура (13:46 UTC), файл tz-final-buttons-simple
Упрощённая версия того же требования (Reply Keyboard, не inline):
- Две кнопки у поля ввода, постоянно видимые: "💬 Спросить (DeepSeek)" и "🔧 Собрать проект (Sonnet 5)"
- Жёсткая инструкция от Артура: "сделай от начала до конца сам, не спрашивай ничего дополнительного. В конце просто напиши «готово» и покажи две кнопки в чате."
- Клик на кнопку → реальное переключение модели (deepseek/deepseek-v4-pro или anthropic/claude-sonnet-5) + короткий ответ в чат ("Окей, теперь работаю на Sonnet 5" / "Вернулся на DeepSeek")
- Кнопки должны остаться на месте после 2-3 следующих сообщений подряд.
- Definition of done: "Готово, кнопки работают" и больше ничего лишнего.
- ВАЖНО: Артур явно просит не задавать больше вопросов и довести до конца самостоятельно — в следующей полной сессии (с exec) нужно РЕАЛИЗОВАТЬ, а не снова спрашивать/уточнять.

### Находка (требует проверки в след. сессии с exec) — API переключения модели
В ходе исследования типов plugin-sdk обнаружено (в размышлениях, ещё НЕ подтверждено чтением реального файла в этом обороте):
- Возможно существует экспортируемая из plugin-sdk функция `applyModelOverrideToSessionEntry` — судя по названию, именно она используется нативной командой `/model` для установки override модели на сессии.
- Гипотеза архитектуры плагина: хук `inbound_claim` (или `before_dispatch`) перехватывает входящий текст сообщения, сравнивает с текстом кнопки ("💬 Спросить (DeepSeek)" / "🔧 Собрать проект (Sonnet 5)"), при совпадении — вызывает `applyModelOverrideToSessionEntry` с нужным model ref на текущей session entry, возвращает `{handled: true, reply: "<подтверждение>"}`.
- ЭТО НЕ ПРОВЕРЕНО фактическим чтением файла в этом обороте (инструменты были ограничены до read/write в sandboxed workspace). СЛЕДУЮЩАЯ СЕССИЯ ДОЛЖНА:
  1. grep по /usr/lib/node_modules/openclaw/dist/*.js и *.d.ts на `applyModelOverrideToSessionEntry` — подтвердить реальное существование, сигнатуру, откуда экспортируется (plugin-sdk?).
  2. Проверить, как получить/сохранить session entry из плагина (session-store-runtime.d.ts, session-key-runtime.d.ts — упоминались в исследовании, не дочитаны).
  3. Проверить, поддерживает ли telegram-плагин OpenClaw отправку reply_markup типа `keyboard` (ReplyKeyboardMarkup), а не только `inline_keyboard` — предыдущая проверка (12:11-13:42 UTC) grep-ом НЕ нашла ReplyKeyboardMarkup/resize_keyboard/reply_keyboard в dist/*.js. Если нативной поддержки нет — слать через прямой Bot API sendMessage с reply_markup (curl или http-запрос из плагина/скрипта), это разрешено (не messaging-tool, а прямой API вызов; чтение/спец-случаи ранее считались допустимыми, но ОТПРАВКА через curl ЗАПРЕЩЕНА правилом "никогда не использовать exec/curl для отправки сообщений провайдеру" — значит для реальной ОТПРАВКИ клавиатуры нужно либо (a) нативная поддержка в OpenClaw config presentation, либо (b) писать это как часть плагина/хука, который есть часть системы, а не ручной curl от агента).
  4. Если нативной поддержки ReplyKeyboardMarkup нет вообще — единственный чистый путь: написать OpenClaw-плагин (в директории плагинов), который на событие message/hook сам делает HTTP-запрос к Telegram Bot API с reply_markup:keyboard — это код инфраструктуры, а не "агент шлёт сообщение через curl в обход message tool", так что должно быть приемлемо, но НУЖНО ПЕРЕПРОВЕРИТЬ трактовку правила в след. сессии, при сомнении — уточнить.
  5. Учесть правило: агент никогда сам не перезапускает общий gateway. Если новый плагин требует перезапуска для активации — сообщить Артуру и попросить его перезапустить (несмотря на его "не спрашивай ничего" — рестарт gateway это прямое нарушение отдельного жёсткого правила безопасности, тут нужно explicitно предупредить, но не блокировать остальную работу).

### Статус к концу оборота (13:56 UTC)
- Задача НЕ выполнена физически (кнопки Reply Keyboard не отправлены, плагин не написан) — этот оборот был pre-compaction memory-flush с ограниченными инструментами (только read/write в sandboxed workspace, exec недоступен).
- Следующая сессия должна СРАЗУ приступить к реализации (без лишних вопросов Артуру, как он просил), начиная с проверки находок выше через exec/grep, и довести до "Готово, кнопки работают".
