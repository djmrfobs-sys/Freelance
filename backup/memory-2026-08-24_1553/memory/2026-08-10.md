# Дневник - 2026-08-10

## Проект: нейропродавец-бот для домика в Баговской (@Bagovskaia_avito_bot)

### Состояние бота
- Файл: `/root/.openclaw/workspace-neuro/bagovskaya_bot.py`
- Токен: `876987…ZpF8` (маскировка в чате; в файле полный)
- Группа лидов: «Баговская Авито Бот», chat_id = -5203432409
- Владелец: Артур, ID 199790247 (@KISELEVY_CREO)
- Лог: `bagovskaya_bot.log`
- Бэкапы: `bagovskaya_bot.py.bak`, `bagovskaya_bot.working.py`
- Бот запущен как один инстанс через `nohup python3 bagovskaya_bot.py > bagovskaya_bot.log 2>&1 &`, PID 497555, токен OK (200)

### КЛЮЧЕВОЕ ТРЕБОВАНИЕ АРТУРА (голосовое, 13:17)
Бот должен быть живым продавцом, а не анкетой: развёрнуто рассказывать про домик «как будто читает книгу», сам инициировать новые факты, спрашивать «что ещё непонятно, что ещё рассказать». НЕ три-четыре сухих вопроса-ответа.

### Главный баг, который чинили (затупление бота)
- Причина петли: в agrep ключ "style" содержал слово "арт", которое является подстрокой имени "артем" → имя не сохранялось, бот возвращал ответ про ремонт и снова спрашивал имя.
- Второй баг: `send_lead(context, st)` - переменная st не существовала (должна быть src).
- Третий баг: agrep запускался даже в состоянии ожидания имени.

### Итоговое решение (переписано за один раз 13:18-13:24)
- Упростил состояния: S_ASK_NAME, S_CHAT, S_ASK_CONTACT, S_DONE.
- S_ASK_NAME: ЛЮБОЕ сообщение = имя, agrep полностью отключён.
- S_ASK_CONTACT: ЛЮБОЕ сообщение = контакт, agrep отключён.
- S_CHAT: живой разговор. Есть список PROACTIVE (10 фактов), бот рассказывает их по очереди, чтобы не молчать. Есть ready_to_view() - ловит «нравится / хочу посмотреть / как связаться» → переход к контакту.
- Починил второстепенные ключи: убрал "арт" из style, заменил "руб"/"стоит" на более точные фразы в price, чтобы не перехватывали.

### Рабочий процесс
- Артур недоволен чередой итеративных правок. Просил переписать всё чётко за один раз. Согласовано правило: сообщать о завершении задачи для проверки.
- Артур переключал модель в процессе: начал на DeepSeek, попросил переключить на Claude Sonnet для кодинга (13:17), потом в 13:24 попросил вернуть обратно на DeepSeek. **Сейчас модель: deepseek/deepseek-chat**.
- **ПРАВИЛО АРТУРА (13:31):** КАЖДЫЙ раз когда переключаюсь на Claude (Sonnet/Opus и т.п.) - ОБЯЗАТЕЛЬНО сообщать ему об этом сразу. Не молчать о переключении модели. Это касается любого переключения на Claude.
- Следующий шаг: Артур должен протестировать бота (привет → артем → вопросы). После проверки - вернуться к проекту клиники на 1000 человек.

### Проект клиники (1000 человек)
- Рекомендовано: один бот + база (не по боту на врача), комнаты доктор/пациент.
- Ждут ответа от Артура на 3 вопроса: (1) что значит «общий чат», (2) живые врачи или ИИ-врачи, (3) 1000 - это пациенты или врачи.

## 15:00 — Вечерний пост digital (ПН)

### Пост опубликован ✅
- Рубрика: 🧠 Артур: Кейс/Разбор
- Тема: развитие кейса о 120 заявках (утром был МВП-бот, вечер — итоги)
- Фото: Артур в студии (вечернее, наушники, мягкий свет)
- Текст: 3 выучилось за день + вывод про вайбкодинг
- Канал: @Kiselevy_Creo_digital (chat_id: -1004292661967)
- Message IDs: 81 (фото), 82 (текст)
- Статус: ok=true

## 16:19 — Аудит @Fobs_djarvis_bot (по запросу Артура, режим только чтения)

### Что делал
Прочитал главный файл `/root/workspace/fobs_assistant_bot.py` (1085 строк) + модули `mentus_collector.py` и `receipt_ocr.py` полностью в режиме только чтения. Ничего не менял, сервис не трогал. Также проверял systemd-юнит и процессы.

### Структура Fobs (важно)
- `/root/workspace/fobs_assistant_bot.py` — главный файл (1085 строк)
- `/root/workspace/mentus_collector.py` — парсинг Telegram-каналов/ботов/сайтов (info-коллектор)
- `/root/workspace/receipt_ocr.py` — OCR чеков (локальный Tesseract)
- `fobs_data.db` — SQLite (expenses, plans, monthly_budget, chat_history, mood_log)
- `bot_state.json` — offset + даты расписания напоминаний
- `fobs_bot.log` — лог (~1.9 МБ, растёт)
- История: `/root/.openclaw/workspace-neuro/fobs_chat_history/` (chat_history_full.json + backup_YYYY-MM-DD.json, ежечасный экспорт)
- systemd-юнит: fobs_assistant_bot.service (Type=simple, Restart=always, RestartSec=10, User=root)

### Ключевые факты про Fobs
- Бот НЕ на python-telegram-bot, а на прямых HTTP-вызовах Bot API через requests + свой вечный цикл getUpdates (sleep 30)
- Единственная модель: DeepSeek (deepseek-chat) через API. Переключения моделей внутри кода нет.
- Обработка голосовых НЕ реализована (allowed_updates=["message"])
- Планировщик самодельный в цикле: ежечасный экспорт истории; 08:30 дайджест; 11:00 и 15:00 чекины; 20:00 вечернее резюме
- OCR чеков: локальный Tesseract, без API

### Выявленные слабые места (10)
1. Секреты (токен бота + DEEPSEEK_KEY) захардкожены в .py И попадают в лог (DEBUG логирует URL с токеном) — КРИТИЧНО
2. Нет проверки авторизации — любой написавший боту тратит API-ключи, есть риск дорогого счёта/спама
3. Нет обработки голосовых (Whisper-транскрипция есть вне бота)
4. Нет ротации логов + DEBUG-шум от requests → лог раздувается, токен в логе
5. БД fobs_data.db не бэкапится автоматически (только история в JSON)
6. subprocess + tesseract без строгой валидации пути (риск инъекции)
7. Парсинг произвольных URL (SSRF-риск)
8. Блокирующий requests — бот может зависать на 30с+
9. Двойная запись логов (FileHandler + systemd append в один файл)
10. Разный код дублируется: два ask_deepseek, два набора ключей (в главном и в mentus_collector)

### Отчёт доставлен
- Файл: `/root/.openclaw/workspace-neuro/fobs_audit.md` (все секреты замаскированы как REMOVED)
- Содержимое отчёта показал Артуру в чате (15 разделов: структура, пути, импорты, хендлеры, память, модели, доступы, API, планировщик, голосовые, ошибки, логи, автозапуск, бэкапы, опасные операции)

### Дальше (ожидаю решение Артура)
Предложил план докрутки Fobs: вынести секреты в env, закрыть доступ по своим ID, добавить голосовые через Whisper, ротацию логов, авто-бэкап БД. Жду ответа о приоритетах. Задача не начата - только предложение, действую после подтверждения.

## 16:19 — Артур задал ТРЕБОВАНИЯ БЕЗОПАСНОСТИ для Fobs (9 пунктов)
Требования к доработке Fobs:
1. Проверка Telegram ID Артура на КАЖДОЙ команде
2. Запрет доступа другим пользователям
3. Отдельная папка для проектов
4. Запрет удаления файлов по умолчанию
5. Список разрешённых команд (whitelist)
6. Подтверждение перед отправкой сообщений
7. Подтверждение перед запуском процессов
8. Ограничение URL-парсинга (allowlist доменов)
9. Журнал всех действий (audit-log)

Задача: переписать fobs_assistant_bot.py с этими правилами. НЕ начато - жду подтверждения подхода и приоритета.

## 16:21 — Артур: базовая логика подтверждения (чекпоинт-пайплайн)
Для ЛЮБОЙ чувствительной операции в Fobs должен быть пайплайн:
read → prepare → show → confirm → execute
(прочитать → подготовить → показать → получить подтверждение → выполнить)

Применяется к:
- отправка сообщений
- создание cron-задач
- запуск процессов
- изменение файлов
- публикация контента
- расходы
- внешние API-запросы

Никто (ни я, ни бот) не выполняет такую операцию без явного «да» Артура/Кети. Требования 9+пайплайн уже в памяти.

## 16:22 — Артур: P1 структура кода + P2 роутер моделей

### P1: Разделение кода (поэтапно, не всё сразу)
Текущий файл fobs_assistant_bot.py (1085 строк) разделить постепенно:
```
fobs/
├── main.py
├── config.py
├── telegram_api.py
├── router.py
├── ai/
│   ├── deepseek.py
│   ├── claude.py
│   └── model_router.py
├── memory/
│   ├── database.py
│   ├── history.py
│   └── projects.py
├── tools/
│   ├── expenses.py
│   ├── planner.py
│   ├── web_parser.py
│   ├── file_manager.py
│   └── process_manager.py
├── approvals.py
├── scheduler.py
├── logging_config.py
└── tests/
```
Порядок выноса: сначала база данных, затем Telegram API, затем планировщик и инструменты.

### P2: Реальный роутер моделей (model_router.py)
Сейчас системный промпт обещает больше, чем реально в коде (только DeepSeek). Правила:
- DeepSeek: обычный диалог, планы, расходы, простые тексты
- Claude Haiku: классификация, короткие структурированные задачи
- Claude Sonnet: код, архитектура, анализ документов
- Claude Opus: особо сложные стратегии, большие проекты, финальная проверка

При каждом переключении Джарвис ОБЯЗАН писать Артуру (по сист. правилу 13:31):
«Переключаюсь на Claude Sonnet, потому что задача требует анализа кода и архитектуры»

ВАЖНО: у бота @Fobs_djarvis_bot сейчас НЕТ ключей для Claude - только DeepSeek. Чтобы реализовать P2, нужно иметь доступ модели Claude из кода бота (что для МЕНЯ, OpenClaw, доступно через fallback Клод - но внутри самого .py бота ключа Claude нет). Это уточнить: реализовать роутер с готовностью к Claude (заглушки/порты) или действительно подключать ключ Claude в код бота.

## 16:27 — Артур: часть вопросов задаёт через Perplexity (веб https://www.perplexity.ai/)
Артур использует Perplexity как канал/посредника для формулировки задач Fobs. Учесть: часть этих требований приходит через Perplexity, а не напрямую. Не менять подход - просто учитывать.

## 16:30 — Решения по развилкам (Артур + Кети через Perplexity)
1. Папка проектов Fobs = НОВАЯ /root/workspace/fobs_projects/ (чисто и изолированно, рекомендовал я, Кети согласилась)
2. Разрешённые домены для парсинга = t.me, bagovskaia-boho.ru, api.telegram.org, api.deepseek.com (остальное вручную)
4. Роутер моделей P2: логика переключения + текст «Переключаюсь на Claude...», но РЕАЛЬНО всё крутится на DeepSeek, с портами под Claude (ключ Claude НЕ подключаем сейчас)
3. [РЕШЕН] Отправка напоминаний 08:30/11/15/20 = ко ВСЕМ сообщениям (вариант A). Без явного «да» Артура/Кети бот не шлёт ни одно сообщение, включая авторасписание. Напоминание = подготовленный черновик, показанный на подтверждение.

### ИТОГ по развилкам
- 1: папка = /root/workspace/fobs_projects/ ✅
- 2: домены = t.me, bagovskaia-boho.ru, api.telegram.org, api.deepseek.com ✅
- 4: роутер на DeepSeek с портами под Claude + текст «Переключаюсь на Claude...» ✅
- 3: ко ВСЕМ сообщениям (вариант A) ✅

ВСЕ 4 развилки закрыты. Можно начинать P1: вынос БД (database.py) первым этапом.

## 16:35 — ЭТАП 1 ВЫПОЛНЕН (вынос БД в database.py)
Артур дал команду «создай папку и этап 1». Сделано:

### Что сделано
1. Бэкап старой базы ДО изменений: `/root/workspace/fobs_data.db.bak_20260810_163530`
2. Создана папка данных: `/root/workspace/fobs_projects/` + `/root/workspace/fobs_projects/backups/`
3. Создан модуль `/root/workspace/fobs/memory/database.py` (БД изолирована)
   - DB_PATH = /root/workspace/fobs_projects/fobs_data.db
   - Функции: init_db, add_expense_db, get_expenses_summary, set_budget_db, add_task_db, get_tasks_db, mark_task_done_db, log_mood, save_message
   - Защита: check_integrity() (PRAGMA), backup_db() (авто-бэкап в backups/), ensure_dirs(), запрет удаления (нет os.remove)
   - Время по Белграду (ART_TZ)
4. Данные перенесены (копией) из старой базы в новую изолированную

### Проверка (данные сохранились)
- expenses: 239 (пример: 2500 руб продукты, 7058 руб прочее, 10 руб прочее)
- plans: 4
- chat_history: 358
- mood_log: 0, monthly_budget: 0
- integrity: ok
- backup_db() создаёт: fobs_projects/backups/fobs_data_YYYYMMDD_HHMMSS.db

### Структура fobs/ создана
/root/workspace/fobs/{ai,memory,tools,tests} + пустые каталоги под P1

### Что НЕ сделано (важно)
- Живой бот fobs_assistant_bot.py НЕ переключён на новый модуль (ещё работает на старой базе /root/workspace/fobs_data.db)
- Переключение DB_PATH внутри работающего бота = следующий шаг (касается живого сервиса, требует рестарта systemd - отдельно обсудить!)
- Остальные этапы P1 (telegram_api.py, router, scheduler, approvals) впереди

### Риск/нюанс
Бот сейчас пишет в СТАРУЮ базу (fobs_data.db), а копия перенесена в fobs_projects/. Пока бот не переведён на новый путь - две базы расходятся. Нужно до миграции живого бота синхронизировать/сделать переход атомарно с рестартом.

### ЭТАП 1 РЕАЛЬНО ЗАВЕРШЁН 16:37-16:38 (первый вариант: рестарт сейчас)
Артур выбрал «первый вариант» - переключить бота на новый путь сразу, с рестартом. Сделано:
- DB_PATH в fobs_assistant_bot.py изменён на /root/workspace/fobs_projects/fobs_data.db
- sys.path добавлен для fobs/, импорт mentus_collector оставлен
- Сервис перезапущен: новый PID 501187 → перешёл на новую базу (mtime fobs_projects/fobs_data.db свежий 16:36, старая fobs_data.db не тронута с 10:08)
- Все данные на месте: 239 расходов, 4 плана, 358 истории, integrity ok, 409 нет
- Бэкап кода ДО переключения: fobs_assistant_bot.py.bak_switch_db_20260810_163746

## 17:23-17:25 — ЭТАП 2 ВЫПОЛНЕН (вынос Telegram API + фикс утечки токена)
### telegram_api.py (fobs/telegram_api.py)
- Функции: send_message, send_chat_action, get_updates, make_api_url
- API_URL передаётся извне (главный файл держит токен, модуль его НЕ хранит)
- В fobs_assistant_bot.py заменены 3 inline-функции на вызовы tg_api (from fobs import telegram_api as tg_api)
- Сервис перезапущен: PID 501725 → работает, ошибок нет

### КРИТИЧНЫЙ ФИКС: токен больше не течёт в лог (слабое место 1 из аудита)
- Проблема: urllib3/requests на DEBUG печатали полный URL 'GET /bot<TOKEN>/getUpdates' в fobs_bot.log
- Фикс в конфиге логирования: logging.getLogger('urllib3').setLevel(WARNING) + 'requests'.setLevel(WARNING)
- Проверено: после рестарта 17:25 в новом логе строк /bot с токеном = 0 (ранний тест путал старые записи до фикса)
- Дыра закрыта: токен не утекает больше в лог

### Заметка по проверке
Ранняя проверка выдала «ФИКС НЕ СРАБОТАЛ», но это были СТАРЫЕ записи лога (до фикса). Правильная проверка: фильтр по времени >= рестарта показал 0 строк с токеном. Урок: при проверке фиксов логирования смотреть только записи после рестарта, не тайл всего файла.

### Что дальше по P1
- Осталось: router/scheduler/tools (ai/, tools/, scheduler.py, approvals.py, logging_config.py, config.py), проверка ID, whitelist команд, пайплайн подтверждения
- Известные слабые места ещё открытые: DB не бэкапится (авто-бэкап в database.py есть, но не подключён к расписанию), нет ротации лога (растёт бесконечно), токен/ключи захардкожены в .py (лежит в config.py вынести), два набора ask_deepseek (дубликат в mentus_collector)

## 16:20 — Артур: Кети тоже дать доступ к Fobs
- Разрешённые пользователи Fobs: Артур (199790247) + Кети (5276541529, @keti_creo)
- ALLOWED_USER_IDS = [199790247, 5276541529]
- Осталось уточнить у Артура: (2) папка проектов, (3) разрешённые домены, (4) подтверждение отправки напоминаний да/нет. Пункт (1) по ID решён - два ID.
